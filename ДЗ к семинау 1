  #Домашнее задание

  # Задача 1: Напишите программу, которая принимает на вход цифру, обозначающую день недели, и проверяет, является ли этот день выходным.
print ( 'Введите число от 1 до 7,соответствующее дню недели: ')
d = int (input())
if d<6:
  print ('Будний день')
elif d==6 or d ==7:
  print ('Выходной день!')
else:
  print ('Недопустимое число')
       

 #Задача 2: Напишите программу для. проверки истинности утверждения ¬(X ⋁ Y ⋁ Z) = ¬X ⋀ ¬Y ⋀ ¬Z для всех значений предикат.

table =[
[False, False, False],
[False, False, True],
[False, True,False],
[True, False, False],
[False,True,True],
[True,True,False],
[True,False,True], 
[True,True,True]
]

for i in range(len(table)):
  for j in range(len(table[i])):
    if not (table[i][0] or table[i][1] or table[i][2]) == (not table[i][0] and not table[i][1] and not table[i][2]):
      itog = True 
    else:
      itog = False  
print (itog)     


#Задача 3: Напишите программу, которая принимает на вход координаты точки (X и Y),
#причём X ≠ 0 и Y ≠ 0 и выдаёт номер четверти плоскости, в которой находится эта точка (или на какой оси она находится).


print ( 'Введите значение координаты Х ')
x = int (input())
print ( 'Введите значение координаты Y ')
y = int (input())

if x>0 and y>0:
  print ( 'Точка принадлежит 1 четверти ')
if x<0 and y<0:
      print ( 'Точка принадлежит 3 четверти ')
if x>0 and y<0:
  print ( 'Точка принадлежит 4 четверти ')
if x<0 and y>0:
  print ( 'Точка принадлежит 2 четверти ')


''' Задача 4:Напишите программу, которая по заданному номеру четверти,
 показывает диапазон возможных координат точек в этой четверти (x и y).'''


print ( 'Введите значение четверти от 1 до 4')
g = int (input())

if g ==1:
  print ( 'Возможны точки с координатами (x>0,y>0)')
if g ==2:
  print ( 'Возможны точки с координатами (x>0,y<0)')
if g ==3:
  print ( 'Возможны точки с координатами (x>0,y<0)')
if g ==4:
  print ( 'Возможны точки с координатами (x<0,y>0)')
if g>4 or g<0:
  print ( 'Введено неверное значение')


#Задача 5: Напишите программу, которая принимает на вход координаты двух точек и находит расстояние между ними в 2D пространстве.

# Точка А (t,r) и точка В ( n,m). Расстояние между 2 точками на плоскости: АВ = sqrt ( (n-t)2 + (m-r)2 )

print ( 'Введите координаты первой точки')
t = int (input())
r = int (input())

print ( 'Введите координаты второй точки')
n = int (input())
m = int (input())

from math import *
def Distance (a,b,c,d):
  AB=sqrt((c-a)**2)+ ((d-b)**2)
  print ('Расстояние между 2 точками на плоскости = ', round(AB))
Distance(t,r,n,m)